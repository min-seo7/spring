package com.yedam;

/*
 * 클래스 = 설계도 : 속성(필드) & 행위(메서드)구성
 * 1. 클래스 선언
 * 2. 인스턴스(객체) 생성 = 메모리 영역 할당
 * 3. 인스턴스 사용
 * 
 * */
public class Car {
	
	String model;
	
	//생성자 - 생성시에 값을 넣어서 생성(=/=기본생성).
	public Car(String model) {
		this.model = model;
	}
	
	
	public void drive() {
		System.out.println("운행");
	}
	
}

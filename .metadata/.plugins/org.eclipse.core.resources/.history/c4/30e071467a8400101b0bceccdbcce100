package com.yedam;

/*
 * 클래스 = 설계도 : 속성(필드) & 행위(메서드)구성
 * 1. 클래스 선언
 * 2. 인스턴스(객체) 생성 = 메모리 영역 할당
 * 3. 인스턴스 사용
 * 
 * */
public class Car {
	//static = 클래스필드 = 모든 인스턴스 공유필드 - (여러개 선언하면 마지막으로 고정됨)
	//static String model;  //해당클래스로 생성되는 모든 인스턴스가 공유하는 필드
	 
	private String model;//private 필드는 직접 접근할 수 없어 getter&setter사용
	
	
	public String getModel() {
		return model;
	}
	
	//생성자 - 객체 생성시 호출되는 메서드, 필드초기화
	//생성자는 return Type없음.
	//생성시에 값을 넣어서 생성(=/=기본생성자)
	
	public Car(String model) {
		this.model = model;
	}
//	//싱글톤
//	private Car(String model) {
//		this.model = model;
//	}
//		
	public void drive() {
		System.out.println(model + " 운행");
	}

	//싱클톤
	// 1. 자기 자신을 저장할 정적 변수
	private static Car instance;

	// 3. 유일한 객체 반환 (필요할 때 생성)
	public static Car getInstance(String model) {
		if (instance == null) {
			instance = new Car(model);
		}
		return instance;
	}
	
	
}
